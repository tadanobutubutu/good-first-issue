{
  "extends": "./tsconfig.json",
  "compilerOptions": {
    "noUnusedLocals": false,
    "noUnusedParameters": false
  },
  "//": "Disabling unused checks in tests to allow for certain test setup variables that are not used directly."
}
